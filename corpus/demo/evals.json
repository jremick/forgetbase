{
  "minimumPassRate": 1,
  "tagMinimumPassRates": {
    "citation-accuracy": 1,
    "policy-compliance": 1,
    "task-completion-quality": 1,
    "restricted-boundary": 1,
    "export-eligibility": 1
  },
  "cases": [
    {
      "id": "eval.pii-redaction-citation",
      "query": "direct personal identifiers support records AI prompt",
      "expectedStableIds": ["guardrail.pii-redaction"],
      "requiredCitationCount": 1,
      "tags": ["privacy", "citation-accuracy"]
    },
    {
      "id": "eval.acceptable-use-policy",
      "query": "What should an agent do if the user requests credential exposure or external posting?",
      "expectedStableIds": ["policy.ai-acceptable-use"],
      "requiredCitationCount": 1,
      "tags": ["policy-compliance", "guardrails"]
    },
    {
      "id": "eval.agent-task-brief-template",
      "query": "agent task brief goal context constraints source of truth acceptance checks",
      "expectedStableIds": ["template.agent-task-brief"],
      "requiredCitationCount": 1,
      "tags": ["task-completion-quality", "agent-operations"]
    },
    {
      "id": "eval.restricted-block-boundary",
      "query": "emergency password access general reader restricted page IDs omitted",
      "expectedStableIds": ["playbook.public-demo-no-export"],
      "requiredCitationCount": 1,
      "tags": ["restricted-boundary", "policy-compliance"],
      "metadata": {
        "forbiddenStableIds": [
          "policy.restricted-credential-handling",
          "runbook.restricted-security-escalation"
        ],
        "note": "Current eval schema cannot enforce forbidden stable IDs; this metadata supports the later leakage verifier and smoke:compose proof."
      }
    },
    {
      "id": "eval.export-eligibility",
      "query": "readable demo page omitted from demo agent pack export access",
      "expectedStableIds": ["playbook.public-demo-no-export"],
      "requiredCitationCount": 1,
      "tags": ["export-eligibility", "governed-distribution"],
      "metadata": {
        "expectedAllowedExports": [],
        "expectedAllowedSurfaces": ["api", "cli", "mcp", "web"],
        "note": "Managed-query evals can retrieve the readable asset; export omission must still be verified by export/leakage checks."
      }
    }
  ]
}
