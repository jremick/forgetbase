services:
  api:
    environment:
      AGENTIC_CMS_SESSION_COOKIE_SECURE: "true"
      AGENTIC_CMS_CORS_ALLOWED_ORIGINS: ${AGENTIC_CMS_CORS_ALLOWED_ORIGINS:-https://127.0.0.1:8443,https://localhost:8443}

  proxy:
    ports:
      - "${AGENTIC_CMS_HTTPS_PORT:-8443}:8443"
    volumes:
      - ./infra/docker/nginx.tls.conf:/etc/nginx/conf.d/default.conf:ro
      - ./infra/docker/tls/tls.crt:/etc/nginx/tls/tls.crt:ro
      - ./infra/docker/tls/tls.key:/etc/nginx/tls/tls.key:ro
