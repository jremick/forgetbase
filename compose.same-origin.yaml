services:
  proxy:
    image: nginx:1.27-alpine
    depends_on:
      - api
      - web
    ports:
      - "${AGENTIC_CMS_PROXY_PORT:-8080}:8080"
    volumes:
      - ./infra/docker/nginx.same-origin.conf:/etc/nginx/conf.d/default.conf:ro
